events {
    worker_connections 1024;
}

http {
    upstream backend {
        server host.docker.internal:8000;
    }

    upstream frontend {
        server host.docker.internal:3000;
    }

    # Define frontend host for proxy header
    map $host $frontend_host {
        default "host.docker.internal:3000";
    }

    upstream ollm {
        # Native mode: Ollama runs on host machine, accessed via host.docker.internal from Docker
        server host.docker.internal:11434;
    }

    upstream neuronpedia {
        # Neuronpedia webapp runs on port 3001 (via host.docker.internal for Docker nginx)
        server host.docker.internal:3001;
    }

    # Map for WebSocket upgrade
    map $http_upgrade $connection_upgrade {
        default upgrade;
        '' close;
    }

    # Dynamic CORS origin - allow both dev and prod domains
    map $http_origin $cors_origin {
        default "";
        "~^https?://mistudio\.hitsai\.(local|net)$" $http_origin;
        "~^https?://dev-mistudio\.hitsai\.local$" $http_origin;
        "~^https?://localhost(:\d+)?$" $http_origin;
        "~^https?://192\.168\.244\.222(:\d+)?$" $http_origin;
    }

    server {
        listen 80;
        server_name mistudio.hitsai.local dev-mistudio.hitsai.local 192.168.224.222 localhost;

        client_max_body_size 100M;

        # oLLM API endpoint (OpenAI-compatible LLM service)
        # Supports /ollama/ for backward compatibility
        location /ollama/ {
            # CORS headers
            add_header 'Access-Control-Allow-Origin' $cors_origin always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, PATCH, DELETE, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization' always;
            add_header 'Access-Control-Expose-Headers' 'Content-Length,Content-Range' always;

            # Handle preflight requests
            if ($request_method = 'OPTIONS') {
                return 204;
            }

            proxy_pass http://ollm/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 300s;
            # Match connect timeout to read timeout — Ollama can be slow to
            # accept connections while loading a large model into VRAM.
            proxy_connect_timeout 300s;

            # Streaming support for chat completions
            proxy_buffering off;
            proxy_cache off;
        }

        # Frontend - proxy to Vite dev server
        location / {
            proxy_pass http://frontend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_set_header Host $frontend_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;

            # Vite HMR websocket
            proxy_read_timeout 86400;
        }

        # Backend API
        # ORDERING NOTE: /api/internal/ MUST appear before /api/ in this file.
        # Nginx uses longest-prefix-match, so the more-specific /api/internal/
        # block takes precedence — moving it below /api/ would silently expose
        # internal Celery→backend endpoints to external callers.
        location /api/internal/ {
            deny all;
            return 403;
        }

        location /api/ {
            # CORS headers
            add_header 'Access-Control-Allow-Origin' $cors_origin always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, PATCH, DELETE, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization' always;
            add_header 'Access-Control-Expose-Headers' 'Content-Length,Content-Range' always;

            # Handle preflight requests
            if ($request_method = 'OPTIONS') {
                return 204;
            }

            proxy_pass http://backend/api/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 10800s;
            proxy_connect_timeout 75s;
            # Buffer tuning: prevents large API responses (feature activations,
            # training metrics) from being spooled to disk under load.
            proxy_buffer_size 16k;
            proxy_buffers 8 16k;
        }

        # WebSocket endpoint for Socket.IO
        location /ws/ {
            # CORS headers for WebSocket
            add_header 'Access-Control-Allow-Origin' $cors_origin always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;

            proxy_pass http://backend/ws/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Socket.IO specific
            proxy_buffering off;
            proxy_cache_bypass $http_upgrade;
            proxy_read_timeout 86400;
        }
    }

    # Neuronpedia server block
    server {
        listen 80;
        server_name dev-neuron.hitsai.local;

        client_max_body_size 100M;

        # Proxy all requests to Neuronpedia webapp
        location / {
            proxy_pass http://neuronpedia;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
            proxy_read_timeout 86400;
        }
    }
}
